1. Error on loading server. Prompted to run "meteor npm install --save babel-runtime". It works.

2. Version: METEOR@1.4.2.3. With "update --all-packages".

3. Using sessions package. "meteor add session".

4. Add d3 "meteor add d3"